name: Validate URLs

on:
  schedule:
    - cron: '0 9 * * 1'  # Every Monday at 9 AM UTC
  workflow_dispatch:     # Allow manual trigger

jobs:
  validate-urls:
    runs-on: ubuntu-latest
    name: Validate repository URLs

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate URLs
        id: validate-urls
        uses: simeg/urlsup-action@v2.1
        with:
          files: '.'                       # Check here
          recursive: true                  # Include sub-dirs
          include-extensions: 'md'         # File types to check

          timeout-seconds: 10              # Request timeout in seconds
          retry: 2                         # Number of retries
          concurrency: 10                  # Concurrent requests

          allow-status: '200,202,204,429'  # Allowed HTTP status codes
          allowlist: 'http://localhost,https://www.valentina-db.com/en/download-valentina-studio,https://www.gnu.org/software/emacs/,https://stackoverflow.com/a/16742333/1564365,https://www.cursor.com/' # Trusted, protected, or flaky URLs

          create-annotations: true         # Show broken URLs in files
          fail-on-error: true              # Fail workflow on broken URLs

          user-agent: 'mac-setup/1.0'

